export enum ConsultationStatus {
    PENDING = 'PENDING',
    IN_PROGRESS = 'IN_PROGRESS',
    RESOLVED = 'RESOLVED',
    CANCELLED = 'CANCELLED',
}

export enum PreferredContact {
    WHATSAPP = 'WHATSAPP',
    TELEGRAM = 'TELEGRAM',
    EMAIL = 'EMAIL',
    CALL = 'CALL',
}